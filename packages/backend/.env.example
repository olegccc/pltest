# port where the service starts
PORT=3000

# local model path (optional - if not provided, rule-based explanations will be used)
# Example: LOCAL_MODEL=model/gemma-3-270m-it-Q6_K.gguf
LOCAL_MODEL=

# path to events CSV file
CSV_PATH=data/events.csv
